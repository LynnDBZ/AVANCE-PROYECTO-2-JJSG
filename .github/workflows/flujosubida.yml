name: Integración y entrega continua

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      nombres: "Josselyn Dahanna"
      apellidos: "Barrientos Zavaleta"
      edad: "20"
